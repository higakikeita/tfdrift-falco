apiVersion: v1
kind: Service
metadata:
  name: {{ include "driftwire.fullname" . }}
  labels:
    {{- include "driftwire.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driftwire.selectorLabels" . | nindent 4 }}
