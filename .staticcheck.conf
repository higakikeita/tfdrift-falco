# Staticcheck configuration
# https://staticcheck.io/docs/configuration

# Note: AWS SDK v1 deprecation warnings (SA1019) are still present
# These should be addressed in a dedicated migration PR to AWS SDK v2
# For now, we accept these warnings as the migration is a large undertaking

checks = ["all"]
